export type Lawyer = {
  image: string;
  name: string;
  surname: string;
};

export type Partner = {
  title: string;
  image: string;
  description: string;
};

export type Blogs = {
  title: string;
  image: string;
  description: string;
};

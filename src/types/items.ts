export type Lawyer = {
  _id: string;
  image: string;
  name: string;
  surname: string;
  description: string
};

export type Partner = {
  _id: string;
  title: string;
  image: string;
  description: string;
};

export type Blogs = {
  _id: string;
  title: string;
  image: string;
  description: string;
};
